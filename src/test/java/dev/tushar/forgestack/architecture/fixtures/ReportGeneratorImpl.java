package dev.tushar.forgestack.architecture.fixtures;

/**
 * Deliberately violates the {@code Impl}-suffix rule. See {@link ReportGenerator}. Do not rename.
 */
class ReportGeneratorImpl implements ReportGenerator {

    @Override
    public String generate() {
        return "";
    }
}
